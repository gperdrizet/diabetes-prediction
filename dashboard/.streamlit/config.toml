[server]
headless = true     # Prevent browser from automatically opening
address = "0.0.0.0" # Bind to all network interfaces to allow LAN access
port = 8501         # Default port

[browser]
gatherUsageStats = false  # Don't automatically open browser
